import React, { Component } from 'react';

const Card = (props) => {
    const {page , random, isWin} = props;
    return (
        <div className = 'card'>
            Card
        </div>
    );
};    

export default Card